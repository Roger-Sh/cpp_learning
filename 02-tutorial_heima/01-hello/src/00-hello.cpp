#include <iostream>
using namespace std;

int main()
{
    // unsigned char upstate[12];

    // union float2char {
    //     float d;
    //     unsigned char data[4];
    // }airstate, Framecheck;

    // // 使用union进行类型转换，int2char
    // airstate.d = 1;
    // Framecheck.d = 1;

    // upstate[0] = 0xA5;
    // upstate[1] = 0xDE;
    // upstate[10] = 0x0D;
    // upstate[11] = 0x0A;

    // for(int i=0;i<4;i++){
    //     upstate[i+2] = airstate.data[i];
    //     upstate[i+6] = Framecheck.data[i];
    // }

    // char result[8];
    // for (int i = 0; i < 12; i++)
    // {
    //     sprintf(result,"%X",upstate[i]);
    //     cout << result[i] << endl;
    // }

    cout << "done!" << endl;

    return 0;

}